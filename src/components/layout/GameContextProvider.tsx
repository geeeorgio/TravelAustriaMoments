import React from 'react';
import { Text, View } from 'react-native';

const GameContextProvider = () => {
  return (
    <View>
      <Text>GameContextProvider</Text>
    </View>
  );
};

export default GameContextProvider;
