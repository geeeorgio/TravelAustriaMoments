import { StyleSheet } from 'react-native';

import { hp, wp } from 'src/utils';

export const styles = StyleSheet.create({
  contentContainer: {
    width: '100%',
    gap: wp(16),
  },
  footer: {
    height: hp(180),
  },
});
